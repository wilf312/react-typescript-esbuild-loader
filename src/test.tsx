// const React = require('react');
// const {App} = require( './App.tsx');
import reactDom from 'react-dom'
import react from 'react'

const dom = document.querySelector('#main') 
if (dom) {
  // reactDom.render(<App />, dom)
  reactDom.render(<div>aaaaaa</div>, dom)
}